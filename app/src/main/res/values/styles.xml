<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme.Base" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimaryVariant">?colorPrimaryDark</item>
        <item name="colorAccent">?colorSecondary</item>
        <item name="android:navigationBarColor">?colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="AppTheme.Splash">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <style name="AppTheme.About" parent="AppTheme.NoActionBar" />

    <!-- needed to show the correct text color on the toolbar -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <!-- needed to show the correct text color on the toolbar -->
    <style name="AppTheme.PopupOverlay" parent="Theme.MaterialComponents.Light" />

</resources>
